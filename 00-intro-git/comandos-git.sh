git init # inicia um repositório git

git add . # adiciona todos os arquivos

git commit -m [Mensagem] # Comita as mudanças com a mensagem 

git add remote [origin] [link] # Cria uma referência do link do repositório

git push -u origin [branch] # Envia os commits para o repositório remoto (Necessário apenas pela primeira vez)